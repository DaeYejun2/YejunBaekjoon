# 백준 11048: 이동하기

https://www.acmicpc.net/problem/11048

<img width="1382" height="1165" alt="image" src="https://github.com/user-attachments/assets/b63a1806-ba33-4444-8d8e-f0eb47428028" />

### 행렬 경로 문제
N x M 크기의 격자에서 특정 지점까지 가는 최적의 비용을 구하는 문제이다.

```
import sys
input = sys.stdin.readline

n,m = map(int,input().split())
data = []
for i in range(n):
    data.append(list(map(int,input().split())))

dp = [[0]*(m+1) for _ in range(n+1)]

for i in range(1,n+1):
    for j in range(1,m+1):
        dp[i][j] = data[i-1][j-1] + max(dp[i-1][j], dp[i][j-1], dp[i-1][j-1])

print(dp[n][m])
```

위, 왼쪽, 대각선 중 최댓값과 data[i-1][j-1]을 더해서 dp[i][j]에 저장해 준다.

경로를 지나면서 이전 값중 가장 큰 값을 계속해서 더해주는 방식.
